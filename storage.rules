rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to images folder for all users
    match /images/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
